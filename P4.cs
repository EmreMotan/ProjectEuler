/*
 * Emre Motan
 * 6/13/2014
 * 
 * Largest palindrome product
 * Problem 4
 * A palindromic number reads the same both ways. The largest palindrome made from the product of two 2-digit numbers is 9009 = 91 × 99.
 * 
 * Find the largest palindrome made from the product of two 3-digit numbers.
*/

namespace ProjectEuler
{
    using System;
    using System.Collections.Generic;
    using System.Linq;
    using System.Text;

    /// <summary>
    /// TODO: Update summary.
    /// </summary>
    public class P4
    {
        /*public void Run()
        {
            int n = 1234;

            // Take a number, convert to string, reverse its characters

            string n_forward = n.ToString();
            
            
            char[] n_array = n_forward.ToCharArray();
            Array.Reverse(n_array);
            string n_backward = new String(n_array);

            if (n_forward == (int)n_backward)
            {
                Console.WriteLine("Palindrome!");
            }

            Console.ReadLine();

        }*/
    }
}
